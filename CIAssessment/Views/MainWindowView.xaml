<Window x:Class="CIAssessment.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CIAssessment"
        mc:Ignorable="d"
        Title="Hierarchical Data Viewer" VerticalAlignment="Center" Height="450" Width="800" WindowStyle="ToolWindow" ResizeMode="NoResize" SizeToContent="Height" WindowStartupLocation="CenterScreen" Cursor="Arrow">
    <Grid>
        <WrapPanel HorizontalAlignment="Center">
            <StackPanel  Margin="200,20,20,5" Orientation="Horizontal" HorizontalAlignment="Center">
                <RadioButton Content="{Binding PartRadioLbl}" HorizontalAlignment="Stretch" Margin="0,0,20,0"/>
                <RadioButton Content="{Binding FileRadioLbl}" HorizontalAlignment="Stretch" Margin="130,0,0,0"/>
            </StackPanel>
            <TabControl ItemsSource="{Binding TabSource}" Margin="10" Height="376" Width="766">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Header}"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>
            </TabControl>
        </WrapPanel>
    </Grid>
</Window>
